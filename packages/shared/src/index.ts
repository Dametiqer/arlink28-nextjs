// zod request/response schemas, API types, and money/currency helpers land
// here starting Phase 2 (docs/monorepo-migration.md) — shared by apps/web,
// apps/admin and apps/api so the three don't drift on request/response shape.

export const SHARED_PACKAGE_PLACEHOLDER = true;
